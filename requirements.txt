numpy>=1.20.0
matplotlib>=3.3.0
scipy>=1.6.0
PyYAML>=5.4.0
pytest>=7.0.0

# Tests only. jsonschema validates live API responses against the schemas
# declared in web/openapi.yaml; 4.18 is where the `referencing` registry these
# tests use arrived. Nothing the service runs on needs it -- see
# web/requirements.txt, which stays Flask and gunicorn alone.
jsonschema>=4.18.0
